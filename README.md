# Streamlit Excel 汇总工具

## 文件说明

- `app.py`：Streamlit 主程序
- `requirements.txt`：依赖列表

## 本地运行

```bash
pip install -r requirements.txt
streamlit run app.py
```

## Community Cloud 部署

把这两个文件推到 GitHub 仓库，然后在 Streamlit Community Cloud 里选择该仓库和 `app.py` 作为入口文件即可。
